
import React from 'react';

// This component is deprecated and removed from the UI.
// Keeping a stub to prevent build errors if referenced elsewhere, 
// though it should be safe to delete.
export const GeminiSettings: React.FC = () => {
    return null;
};
